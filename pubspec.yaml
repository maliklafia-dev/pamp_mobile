name: pamp_mobile
description: "PAMP mobile application."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.0.5
  intl: ^0.20.2
  http: ^1.2.1
  shared_preferences: ^2.2.2
  flutter_secure_storage: ^9.1.0
  url_launcher: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
flutter:

  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/